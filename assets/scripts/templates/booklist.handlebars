{{#each books as |book|}}
 <!-- Developers, why are we using data-id and not just id? -->
 <ul data-id="{{book.id}}">
    <li>Title: {{book.title}}</li>
    <li>
      Author: {{book.author}}
    </li>
  </ul>
{{/each}}
